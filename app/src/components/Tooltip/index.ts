import { _TooltipContent } from './content';
import { _TooltipProvider, _TooltipRoot } from './root';
import { _TooltipTrigger } from './trigger';

export const Tooltip = {
  Provider: _TooltipProvider,
  Root: _TooltipRoot,
  Trigger: _TooltipTrigger,
  Content: _TooltipContent,
};
